import { IngeleverdPipe } from './ingeleverd.pipe';

describe('IngeleverdPipe', () => {
  it('create an instance', () => {
    const pipe = new IngeleverdPipe();
    expect(pipe).toBeTruthy();
  });
});
