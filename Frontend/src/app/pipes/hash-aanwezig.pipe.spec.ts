import { HashAanwezigPipe } from './hash-aanwezig.pipe';

describe('HashAanwezigPipe', () => {
  it('create an instance', () => {
    const pipe = new HashAanwezigPipe();
    expect(pipe).toBeTruthy();
  });
});
